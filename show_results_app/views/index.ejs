<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ACIT3495 Project - View Analytics</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">View Metric Analytics</h1>
        <p>Enter your credentials and select a metric to view its computed analytics.</p>
        <div class="card">
            <div class="card-body">
                <form action="/show" method="POST">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" value="bob" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" value="bob123" required>
                    </div>
                    <div class="mb-3">
                        <label for="metric" class="form-label">Metric</label>
                        <select class="form-select" id="metric" name="metric">
                            <option value="grade">Grade</option>
                            <option value="temperature">Temperature</option>
                            <option value="humidity">Humidity</option>
                            <option value="pressure">Pressure</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">Show Results</button>
                </form>
            </div>
        </div>
        <% if (error) { %>
            <div class="alert alert-danger mt-4" role="alert">
                <strong>Error:</strong> <%= error %>
            </div>
        <% } %>
        <% if (result) { %>
            <div class="card mt-4">
                <div class="card-header">
                    <h4>Analytics for: <span class="fw-bold text-capitalize"><%= result.metric %></span></h4>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-hover">
                        <tbody>
                            <tr>
                                <th>Minimum Value</th>
                                <td><%= result.min %></td>
                            </tr>
                            <tr>
                                <th>Maximum Value</th>
                                <td><%= result.max %></td>
                            </tr>
                            <tr>
                                <th>Average Value</th>
                                <td><%= result.avg.toFixed(2) %></td>
                            </tr>
                            <tr>
                                <th>Total Count</th>
                                <td><%= result.count %></td>
                            </tr>
                            <tr>
                                <th>Last Updated (UTC)</th>
                                <td><%= new Date(result.updated_at).toLocaleString('en-CA', { timeZone: 'UTC' }) %></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        <% } %>

    </div>
</body>
</html>